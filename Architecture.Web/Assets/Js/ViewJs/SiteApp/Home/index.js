controllers.HomeController = ['$scope', '$http', '$element', '$filter',
    function ($scope, $http, $element, $filter) {

    }];

$(document).ready(function () {
    ShowPageLoadMessage("ShowLoginSuccessMessage");
});
