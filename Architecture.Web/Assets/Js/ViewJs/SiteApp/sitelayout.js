controllers.SiteLayoutController = ['$scope', '$http', '$timeout', function ($scope, $http, $timeout) {

    //TODO
    //This is the parent controller for every controller those are extends _SiteLayout.cshtml
    //You can do the common code here and access into the child conroller.
}];

$(document).ready(function () {

});